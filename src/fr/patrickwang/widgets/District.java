package fr.patrickwang.widgets;

import javax.swing.*;

public class District extends JButton {
    public District(ImageIcon imageIcon) {
        super(imageIcon);
        setBorder(BorderFactory.createEmptyBorder());
        setContentAreaFilled(false);
    }
}
